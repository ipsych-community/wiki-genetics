```{r setup, include=FALSE}
# Global {knitr} options
source("knitr-options.R")
knitr::opts_chunk$set()
# Spelling check
WORDS_TO_IGNORE <- c()
source("spelling-check.R")
```

# Computing genetic correlations with SumHer

The key difference between SumHer and the existing software LD Score Regression (LDSC) is that SumHer allows the user to specify the heritability model [@speed2019sumher].

[TODO]

